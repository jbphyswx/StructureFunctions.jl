```@meta
CurrentModule = StructureFunctions
```

# Result containers

Binned results. Raw accumulators carry the `…SumsAndCounts` suffix and add across processes and time
steps; the bare names are the averaged views.

```@index
Pages = ["results.md"]
```

```@autodocs
Modules = [StructureFunctions.StructureFunctionObjects]
Private = false
Order   = [:type, :function]
```
